{{ if ne .Values.environment "ci" }}
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: {{ include "cla.fullname" . }}
spec:
  secretName: {{ include "cla.fullname" . }}-tls
  issuerRef:
    name: letsencrypt
  dnsNames:
    {{ .Values.claAssistant.host }}
  acme:
    config:
    - dns01:
        provider: prod-dns
      domains: {{ .Values.claAssistant.host }}
{{ end }}
